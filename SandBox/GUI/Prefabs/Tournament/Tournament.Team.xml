<Prefab>
	<Parameters>
		<Parameter Name="OnMission" DefaultValue="false" />
	</Parameters>
	<Constants>
	</Constants>
	<VisualDefinitions>
	</VisualDefinitions>
	<Window>
		<Widget WidthSizePolicy="StretchToParent" HeightSizePolicy="CoverChildren" IsVisible="@IsValid" Brush="Tournament.Team" >
			<Children>

				<ListPanel WidthSizePolicy="StretchToParent" HeightSizePolicy="CoverChildren" VerticalAlignment="Center" LayoutImp.LayoutMethod="VerticalTopToBottom">
					<Children>
						<Tournament.Participant DataSource="{Participant1}" Parameter.OnMission="*OnMission"/>
						<Tournament.Participant DataSource="{Participant2}" Parameter.OnMission="*OnMission"/>
						<Tournament.Participant DataSource="{Participant3}" Parameter.OnMission="*OnMission"/>
						<Tournament.Participant DataSource="{Participant4}" Parameter.OnMission="*OnMission"/>
						<Tournament.Participant DataSource="{Participant5}" Parameter.OnMission="*OnMission"/>
						<Tournament.Participant DataSource="{Participant6}" Parameter.OnMission="*OnMission"/>
						<Tournament.Participant DataSource="{Participant7}" Parameter.OnMission="*OnMission"/>
						<Tournament.Participant DataSource="{Participant8}" Parameter.OnMission="*OnMission"/>
					</Children>
				</ListPanel>
			
			
			</Children>								
		</Widget>
	</Window>
</Prefab>